# Root .env — shared vars
NODE_ENV=development

# Database — shared across services
DB_HOST=localhost
DB_PORT=5432
DB_NAME=hhnewdb
DB_USER=postgres
DB_PASSWORD=postgres

# Redis — shared
REDIS_URL=redis://localhost:6379

# Per-service ports (each service reads its own PORT var)
SERVICE_NAME_PORT=3000
